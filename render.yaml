services:
  - type: web
    name: sistema-financeiro
    runtime: python
    buildCommand: cd backend && chmod +x build.sh && ./build.sh
    startCommand: cd backend && python -m gunicorn app.main:app --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: DATABASE_URL
        value: postgresql://postgres.zbfhalzyyfobldzfnioy:FFyVPjKDJEJDeu4q@aws-0-us-west-2.pooler.supabase.com:6543/postgres
